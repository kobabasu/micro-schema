/**
 * node_modulesが存在するディレクトリからの
 * ドキュメントルートを指定
 *
 * nameはtask名の先頭になる
 */

const root = './admin/docs/schema/';

export const dir = {
  name: '',

  root: root,

  concat: {
    cnf: root + 'config.xml',
    xml: root + 'mysqldump.xml'
  },

  tables: {
    pdf: {
      src: root + 'tables/source/pdf/',
      build: root + 'tables/build/pdf/'
    },
    html: {
      src: root + 'tables/html/',
      css: root + 'tables/html/css/'
    }
  },

  masters: {
    pdf: {
      src: root + 'masters/source/pdf/',
      build: root + 'masters/build/pdf/'
    },
    html: {
      src: root + 'masters/html/',
      css: root + 'masters/html/css/'
    }
  },

  views: {
    pdf: {
      src: root + 'views/source/pdf/',
      build: root + 'views/build/pdf/'
    },
    html: {
      src: root + 'views/html/',
      css: root + 'views/html/css/'
    }
  },

  procedures: {
    pdf: {
      src: root + 'procedures/source/pdf/',
      build: root + 'procedures/build/pdf/'
    },
    html: {
      src: root + 'procedures/html/',
      css: root + 'procedures/html/css/'
    }
  }
}
